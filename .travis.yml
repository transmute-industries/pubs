matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required

before_install:
  - git clone https://github.com/sstephenson/bats.git \
    && this_pwd=$(pwd) cd bats \
    && cd $this_pwd \
    && sudo  ./install.sh /usr/local

script:
  - bats .ci/tests.bats
